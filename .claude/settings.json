{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bunx ccdont --deny-bash 'rm [prompt user to exec]' --deny-bash 'bun test [use \"bun run test\" instead.]' --deny-bash 'npm [use bun instead.]'"
          }
        ]
      }
    ]
  }
}